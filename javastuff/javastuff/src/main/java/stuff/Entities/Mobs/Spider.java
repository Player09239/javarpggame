package stuff.Entities.Mobs;

import stuff.Entities.Enemy;

public class Spider extends Enemy {
    public Spider(int maxhp, int atk, int mincash, int maxcash) {
        super(maxhp, atk, "Spider", mincash, maxcash);
    }
}
